# 🏗️ Архитектура проекта

## Обзор

```
┌─────────────────────────────────────────────────────────────┐
│                      Telegram API                           │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                     aiogram 3.x                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │  Handlers   │  │ Middlewares │  │     FSM States      │  │
│  └──────┬──────┘  └──────┬──────┘  └──────────┬──────────┘  │
└─────────┼────────────────┼─────────────────────┼────────────┘
          │                │                     │
┌─────────▼────────────────▼─────────────────────▼────────────┐
│                    Business Logic                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │  Analytics  │  │   Payments  │  │    AI Generators    │  │
│  └──────┬──────┘  └──────┬──────┘  └──────────┬──────────┘  │
└─────────┼────────────────┼─────────────────────┼────────────┘
          │                │                     │
┌─────────▼────────────────▼─────────────────────▼────────────┐
│                     Data Layer                               │
│  ┌─────────────────────┐  ┌─────────────────────────────┐   │
│  │   SQLAlchemy ORM    │  │       RouterAI (Claude)     │   │
│  └──────────┬──────────┘  └──────────────┬──────────────┘   │
└─────────────┼────────────────────────────┼──────────────────┘
              │                            │
      ┌───────▼───────┐            ┌───────▼───────┐
      │  PostgreSQL   │            │  RouterAI API │
      │  (Supabase)   │            │  (Claude/GPT) │
      └───────────────┘            └───────────────┘
```

---

## Модули

### `src/bot/` — Telegram Bot

| Файл | Описание |
|------|----------|
| `main.py` | Точка входа, инициализация |
| `states.py` | FSM состояния диагностики |
| `scheduler.py` | Планировщик напоминаний |
| `handlers/` | Обработчики команд |
| `keyboards/` | Inline клавиатуры |
| `middlewares/` | Логирование, ошибки |

### `src/ai/` — AI Integration

| Файл | Описание |
|------|----------|
| `client.py` | OpenAI-совместимый клиент |
| `question_gen.py` | Генерация адаптивных вопросов |
| `answer_analyzer.py` | Анализ ответов пользователя |
| `report_gen.py` | Генерация итогового отчёта |
| `cached_questions.py` | Кэш первых вопросов |

### `src/db/` — Database

| Файл | Описание |
|------|----------|
| `models.py` | SQLAlchemy ORM модели |
| `session.py` | Async session factory |
| `repositories/` | CRUD операции |

### `src/analytics/` — Analytics

| Файл | Описание |
|------|----------|
| `dynamics.py` | Динамика между сессиями |
| `benchmark.py` | Сравнение с рынком |
| `pdp_generator.py` | Генератор PDP |
| `competency_profile.py` | Профиль компетенций |

### `src/utils/` — Utilities

| Файл | Описание |
|------|----------|
| `pdf_generator.py` | Генерация PDF отчётов |
| `message_splitter.py` | Разбивка длинных сообщений |
| `share_card.py` | Генерация share cards |

---

## Диаграмма БД

```
┌──────────────┐     ┌────────────────────┐     ┌─────────────────┐
│    users     │────<│ diagnostic_sessions │────<│     answers     │
└──────────────┘     └────────────────────┘     └─────────────────┘
       │                      │
       │              ┌───────┴───────┐
       │              │               │
       ▼              ▼               ▼
┌──────────────┐  ┌──────────┐  ┌───────────────┐
│ user_balances│  │ pdp_plans│  │   feedbacks   │
└──────────────┘  └────┬─────┘  └───────────────┘
       │               │
       │               ▼
┌──────────────┐  ┌──────────┐
│   payments   │  │ pdp_tasks│
└──────────────┘  └──────────┘
```

---

## Flow диагностики

```
/start
   │
   ▼
Выбор роли (designer/product)
   │
   ▼
Выбор опыта (junior/middle/senior/lead)
   │
   ▼
Проверка доступа (demo/full/paywall)
   │
   ▼
Онбординг (правила, пример)
   │
   ▼
┌──────────────────────────────┐
│   Цикл вопросов (3 или 10)   │◄──┐
│   1. Генерация вопроса (AI)  │   │
│   2. Ответ пользователя      │   │
│   3. Анализ ответа (AI)      │   │
│   4. Следующий вопрос        │───┘
└──────────────────────────────┘
   │
   ▼
Генерация отчёта (AI)
   │
   ▼
Генерация PDF
   │
   ▼
Сохранение в БД
   │
   ▼
Отправка пользователю
```

