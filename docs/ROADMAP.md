# 🗺️ Deep Audit & Technical Roadmap

> Этот документ составлен на основе глубинного аудита кода от 2025-12-30.
> Он содержит список критических проблем, архитектурных улучшений и план развития.

---

 

Эти задачи необходимо выполнить в первую очередь, так как они влияют на стабильность, безопасность и сохранение данных.

### 1.1. Безопасность и Конфигурация
- [x] **Вынести Sentry DSN в .env**: Используется `config.sentry_dsn` в `src/bot/main.py`.
- [x] **Вынести `ADMIN_ID` в .env**: Используется `config.admin_telegram_id`.
- [x] **Убрать локальное логирование AI**: Переведено на stdout (JSON logs) для Docker.

### 1.2. Сохранение состояния (State Management)
- [x] **Заменить `MemoryStorage` на `RedisStorage`**:
  - Сейчас: Используется `RedisStorage` в `src/bot/main.py`.
- [x] **Персистентные напоминания**:
  - Реализовано хранение напоминаний в БД (`DiagnosticReminder`) и проверка через планировщик.

### 1.3. Обработка ошибок AI
- [x] **Улучшить JSON Parsing**: `robust_json_parse` в `src/ai/answer_analyzer.py` обрабатывает markdown блоки и ошибки структуры.
- [x] **Graceful Degradation**: При ошибке генерации вопроса (`generate_question`) используется умный fallback на подготовленные вопросы по роли (вместо падения или generic вопросов).

---

## 🏗️ Phase 2: Architecture Refactoring (Next 2 Weeks)

Улучшение архитектуры для масштабируемости и поддерживаемости.

### 2.1. Платежная система
- [x] **Вынести цены в БД или Config**: `PACK_PRICES` захардкожены в коде. Изменение цены требует деплоя.
- [x] **Идемпотентность**: Убедиться, что webhook от платежки обрабатывается ровно один раз (сейчас базовая защита есть, но нужен request ID check).

### 2.2. Планировщик (Scheduler)
- [x] **Рефакторинг `scheduler.py`**: Текущий `while True` цикл в основном процессе — плохое решение.
  - [x] Выделить планировщик в отдельный сервис/контейнер или использовать `APScheduler` async mode.
  - [x] Обеспечить восстановление пропущенных задач после перезапуска.

### 2.3. База данных
- [x] **Транзакционность**: Проверить `create_session` и `update_session_progress`. Обеспечить атомарность операций (особенно при списании баланса).
- [x] **Alembic**: Настроить Alembic миграции для автоматического деплоя (сейчас только ручной `alembic upgrade head`).

---

## 🕵️ Phase 3: QA & Testing Strategy (Detailed)

Раздел добавлен профессиональным тестировщиком для обеспечения качества продукта.

### 3.1. Функциональное тестирование (Manual)
- [x] **Edge Cases (Граничные случаи)**:
  - [x] **Пустые сообщения**: Отправка пустого текста, стикеров, файлов вместо текстового ответа на вопрос.
  - [x] **Слишком длинные сообщения**: Telegram имеет лимит 4096 символов. Проверить, как бот обрабатывает ответы > 4000 символов (разбивка или ошибка).
  - [x] **Double Click**: Быстрое двойное нажатие на кнопки (inline keyboard). Может привести к двойному списанию баланса или пропуску вопроса.
  - [x] **Race Conditions**: Отправка нового сообщения пока бот "думает" (генерирует ответ AI). Бот должен игнорировать ввод или ставить в очередь.

- [ ] **Payment Flow (Платежи)**:
  - [ ] **Отмена оплаты**: Пользователь нажал "Купить", получил invoice, но не оплатил.
  - [ ] **Ошибка провайдера**: Эмуляция сбоя на стороне ЮKassa/Telegram.
  - [ ] **Повторная покупка**: Покупка того же пакета, когда предыдущий еще не израсходован.

### 3.2. Негативное тестирование (Negative Testing)
- [x] **AI Failures**:
  - [x] **Timeout**: Эмуляция задержки ответа AI > 60 секунд. Бот должен извиниться и предложить повторить, а не "висеть".
  - [x] **Bad JSON**: Эмуляция ответа AI, который невозможно распарсить. Должен сработать механизм retry или fallback.
  - [x] **Hallucinations**: Проверка на то, что бот не выдает несуществующие команды или ссылки.

- [x] **State Recovery**:
  - [x] **Stuck State**: Что если бот упал во время генерации отчета (`generating_report`)? Пользователь останется в этом состоянии навсегда? Нужен механизм сброса (/start).
  - [x] **Block Bot**: Пользователь заблокировал бота, затем разблокировал. Напоминания не должны вызывать 500 ошибку в логах.

### 3.3. Нагрузочное тестирование
- [ ] **Concurrent Users**: Запуск 50-100 одновременных диалогов (можно использовать `locust` или скрипты на `pyrogram`).
- [ ] **Memory Leaks**: Мониторинг потребления RAM при длительной работе (особенно из-за `_reminder_tasks` словаря).

### 3.4. Автоматизация (Automated Testing)
- [ ] **Unit Tests**:
  - [x] `src/ai/answer_analyzer.py`: Тесты на парсинг разных форматов JSON (с markdown, без, с мусором).
  - [x] `src/bot/scheduler.py`: Тесты на форматирование напоминаний.
  - [x] `src/utils/message_splitter.py`: Тесты на разбивку длинных сообщений.
- [ ] **Integration Tests**:
  - [x] БД: Создание пользователя -> Создание сессии -> Сохранение ответа.
  - [x] Flow: Эмуляция прохождения полного цикла (mocking AI).

---

## 🧠 Phase 4: AI Quality & Core Product (Ongoing)

Улучшение качества диагностики.

### 4.1. Промпт-инжиниринг
- [x] **Анализ промптов**: В `src/core/prompts/` добавить версионирование промптов.
- [x] **Очистка артефактов**: Убрать `strip().strip('"')` из кода, заставив модель выдавать чистый результат промптом.
- [x] **Structured Outputs**: Перейти на строгую схему JSON Schema для ответов модели.

### 4.2. Качество вопросов
- [x] **Review Hardcoded Questions**: База вопросов расширена, добавлены рандомные ice-breakers для старта.
- [x] **Context Window**: Убедиться, что `conversation_history` не переполняет контекст модели на 10-м вопросе (токен-лимит).

---

## 🚀 Phase 5: Features & UX (Future)

Развитие функционала.

### 5.1. Аналитика для пользователя
- [ ] **Личный кабинет**: Web App (Mini App) для просмотра истории и прогресса.
- [ ] **Сравнение с рынком**: Реализовать `benchmark.py` на реальных данных (сейчас, вероятно, заглушки или мало данных).

### 5.2. Геймификация
- [ ] **Leaderboard**: Топ по баллам (опционально, анонимно).
- [ ] **Achievements**: Сохранять достижения в БД, а не только генерировать в тексте.

---

## 🛠️ Technical Debt & Cleanup

- [x] **Docker Optimization**: `Dockerfile` uses multi-stage build and non-root user. Optimized layers.
- [x] **Testing**: Unit tests for `src/ai/answer_analyzer.py` and `src/bot/scheduler.py` added.
- [x] **Logging**: Structured logging (JSON) implemented via `python-json-logger`. Configurable via `LOG_FORMAT`.

---

## 📊 Summary Execution Plan

1. **Week 1**: Fix Sentry/Env, Switch to Redis FSM, Refactor Scheduler.
2. **Week 2**: QA Audit (Manual Testing), Refactor Payments, Fix JSON Parsing.
3. **Week 3**: Automated Tests, AI Prompt Tuning, Web App Prototype.
