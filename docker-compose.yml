services:
  bot:
    build: .
    container_name: diagnostic-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=Europe/Moscow
      - REDIS_URL=redis://redis:6379/0
      - DATABASE_URL=postgresql+asyncpg://user:password@db:5432/diagnostic_bot
    volumes:
      - ./debug_logs:/app/debug_logs
      - ./migrations:/app/migrations
      - ./logs:/app/logs
    depends_on:
      redis:
        condition: service_healthy
      db:
        condition: service_healthy

  watchdog:
    build: .
    user: root
    container_name: diagnostic-watchdog
    restart: unless-stopped
    command: python -u scripts/watchdog.py
    env_file:
      - .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - bot

  redis:
    image: public.ecr.aws/docker/library/redis:7-alpine
    container_name: diagnostic-redis
    restart: unless-stopped
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  db:
    image: public.ecr.aws/docker/library/postgres:15-alpine
    container_name: diagnostic-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: diagnostic_bot
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d diagnostic_bot"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  redis_data:
  postgres_data:
