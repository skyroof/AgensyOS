# 🤝 Руководство по контрибьютингу

Спасибо за интерес к проекту! Вот как можно помочь.

---

## 🚀 Быстрый старт

### 1. Fork и клонирование

```bash
git clone https://github.com/YOUR_USERNAME/AgensyOS.git
cd AgensyOS
```

### 2. Настройка окружения

```bash
python -m venv venv
venv\Scripts\activate  # Windows
pip install -r requirements.txt
cp env-example.txt .env
# Заполни .env своими данными
```

### 3. Запуск локально

```bash
python -m src.bot.main
```

---

## 📋 Процесс разработки

### Ветки

- `main` — продакшен (защищённая)
- `develop` — разработка
- `feature/*` — новые фичи
- `fix/*` — исправления
- `hotfix/*` — срочные фиксы в продакшен

### Коммиты

Используем [Conventional Commits](https://www.conventionalcommits.org/):

```
feat: add payment history page
fix: correct balance display on /start
docs: update README with deploy instructions
refactor: simplify diagnostic flow
style: format code with black
test: add unit tests for balance_repo
chore: update dependencies
```

### Pull Request

1. Создай ветку от `develop`
2. Сделай изменения
3. Проверь что бот запускается без ошибок
4. Создай PR в `develop`
5. Опиши что сделано и зачем

---

## 🔧 Стиль кода

### Python

- Форматтер: `black`
- Линтер: `ruff` или `flake8`
- Тип-хинты обязательны
- Docstrings для публичных функций

```python
async def get_user_balance(session: AsyncSession, user_id: int) -> UserBalance:
    """
    Получить баланс пользователя.
    
    Args:
        session: Сессия БД
        user_id: ID пользователя в БД
        
    Returns:
        Объект баланса пользователя
    """
    ...
```

### Telegram сообщения

- Используй HTML, не Markdown
- Помни про лимит 4096 символов
- Для длинных сообщений — `send_long_message()`

---

## 📁 Куда добавлять код

| Что | Куда |
|-----|------|
| Новая команда бота | `src/bot/handlers/` |
| Новая inline-клавиатура | `src/bot/keyboards/inline.py` |
| Новая модель БД | `src/db/models.py` |
| CRUD для модели | `src/db/repositories/` |
| AI-логика | `src/ai/` |
| Аналитика | `src/analytics/` |
| Утилиты | `src/utils/` |

---

## ⚠️ Важно

### Не коммить

- `.env` файлы
- `*.pyc`, `__pycache__/`
- IDE настройки (`.idea/`, `.vscode/`)
- Локальные БД (`*.db`)

### Тестирование

Перед PR обязательно проверь:

1. Бот запускается без ошибок
2. `/start` работает
3. Твоя фича работает
4. Нет регрессий в основном флоу

---

## 💬 Вопросы?

Создай Issue или напиши в Telegram: [@laitnerbro](https://t.me/laitnerbro)

